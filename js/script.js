$(document).ready(
            function (){
              $('.first-button-menu').click(function (){
                        $('.navbar').toggleClass('hidden');
                        $('.first-button-menu-line').toggleClass('orange');
                      }

                )
            }
    );